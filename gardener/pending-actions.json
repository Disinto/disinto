[
  {
    "action": "comment",
    "issue": 1431,
    "body": "This fix is already in place. The root AGENTS.md Directory layout now reads: 'the executor is `*-run.sh` (gardener, supervisor, planner, predictor, architect) — dev/ uses `dev-agent.sh`/`dev-poll.sh` and review/ uses `review-pr.sh`/`review-poll.sh`' — dev/ and review/ are correctly distinguished. Merged via #1437."
  },
  {
    "action": "close",
    "issue": 1431,
    "reason": "already implemented — root AGENTS.md already distinguishes dev/review executors (fix merged in #1437)"
  },
  {
    "action": "comment",
    "issue": 1427,
    "body": "This fix is already in place. The `lib/tape.sh` row in `lib/AGENTS.md` now lists all four direct sourcers — dev/dev-poll.sh (#1398), lib/formula-session.sh (#1391), docker/edge/dispatcher.sh (#1407), supervisor/supervisor-run.sh (#1422) — plus tools/grade.sh (#1410). Merged via #1437."
  },
  {
    "action": "close",
    "issue": 1427,
    "reason": "already implemented — lib/AGENTS.md tape.sh Sourced-by column now lists all direct consumers (fix merged in #1437)"
  },
  {
    "action": "comment",
    "issue": 1412,
    "body": "This fix is already in place. The host_volume contract header in `nomad/jobs/agents-dev-qwen.hcl` now lists all five mounts: agent-data, project-repos, ops-repo, factory-projects, and tape. Merged via #1437."
  },
  {
    "action": "close",
    "issue": 1412,
    "reason": "already implemented — hcl host_volume header now lists all five mounts (fix merged in #1437)"
  }
]
